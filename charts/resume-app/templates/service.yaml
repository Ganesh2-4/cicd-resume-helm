apiVersion: v1
kind: Service
metadata:
  name: resume-app-svc
spec:
  type: {{ .Values.service.type }}
  selector:
    app: resume-app
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
